import { Injectable } from '@angular/core';
import { Item } from '../_models/Item';

@Injectable({
  providedIn: 'root'
})
export class OrderService {

  constructor() { }

  add(items: Item[]) {
    
  }
}
